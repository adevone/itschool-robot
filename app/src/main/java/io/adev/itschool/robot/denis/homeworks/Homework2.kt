package io.adev.itschool.robot.denis.homeworks

import io.adev.itschool.robot.*
import io.adev.itschool.robot.global.*

fun denisLevel4(){
    // вызываем функцию display, передавая ей "p62" в качестве параметра password
    display("p62")
    // вызываем функцию moveRight, передавая ей значение 6 параметра stepsCount
    moveRight(stepsCount = 6)
    // вызываем функцию moveDown, передавая ей значение 2 параметра stepsCount
    moveDown(stepsCount = 2)
    // вызываем функцию display, передавая ей "p14" в качестве параметра password
    display("p14")
    // вызываем функцию moveLeft, передавая ей значение 1 параметра stepsCount
    moveLeft(stepsCount = 1)
    // вызываем функцию moveDown, передавая ей значение 1 параметра stepsCount
    moveDown(stepsCount = 1)
    // вызываем функцию moveLeft, передавая ей значение 2 параметра stepsCount
    moveLeft(stepsCount = 2)
    // вызываем функцию moveUp, передавая ей значение 1 параметра stepsCount
    moveUp(stepsCount = 1)
    // вызываем функцию moveLeft, передавая ей значение 2 параметра stepsCount
    moveLeft(stepsCount = 2)
    // вызываем функцию moveDown, передавая ей значение 2 параметра stepsCount
    moveDown(stepsCount = 2)
    // вызываем функцию moveRight, передавая ей значение 1 параметра stepsCount
    moveRight(stepsCount = 1)
    // вызываем функцию display, передавая ей "p56" в качестве параметра password
    display("p56")
    // вызываем функцию moveDown, передавая ей значение 1 параметра stepsCount
    moveDown(stepsCount = 1)
    // вызываем функцию moveRight, передавая ей значение 6 параметра stepsCount
    moveRight(stepsCount = 6)
    // вызываем функцию moveUp, передавая ей значение 1 параметра stepsCount
    moveUp(stepsCount = 1)
}